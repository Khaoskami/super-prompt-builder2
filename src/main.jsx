import React from 'react'
import ReactDOM from 'react-dom/client'
import SuperPromptBuilder from './SuperPromptBuilder'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SuperPromptBuilder />
  </React.StrictMode>
)
